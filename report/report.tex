\documentclass[11pt]{article}

% Basic packages
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage[stretch = 25, shrink = 25, tracking=true, letterspace=30]{microtype}

\usepackage{geometry}
 \geometry{
 a4paper,
 %total={150mm,257mm},
 left=25mm,
 right=25mm,
 top=15mm,
 } % Page Layout
\usepackage[dvipsnames]{xcolor} % Colour
\usepackage{amsmath} % Maths

%%%%%%%%%%%%%%%%%%%
% Tables, figures %
%%%%%%%%%%%%%%%%%%%

\usepackage{graphicx} % Imgages, figures
\graphicspath{{./figures/}} % Set figure path

\usepackage{booktabs} % Pretty tables

%%%%%%%%%%%%%
% Citations %
%%%%%%%%%%%%%

\usepackage[
    backend=biber,
    style=apa,
    sortlocale=de_DE,
    natbib=true,
    url=false, 
    doi=true,
    eprint=false
]{biblatex}

\addbibresource{Bandit.bib} %Imports bibliography file
\usepackage{csquotes} % used for block quotes

%%% Helvetica:
%\usepackage[scaled=0.95]{helvet} % Imports Helvetica, which is similar to Arial
%\renewcommand{\rmdefault}{phv} % Use Helvetica

%%% Atkinson Hyperlegible, nerdy font 
%\usepackage[sfdefault]{atkinson} %% Option 'sfdefault' if the base
%% font of the document is to be sans serif.
%\usepackage[T1]{fontenc}

\usepackage{titlesec} % pretty headers
\titleformat{\section}
  {\normalfont\large\bfseries\color{Black}} % Formatting options
  {\thesection}{1em}{}

\title{\textcolor{ForestGreen}{\textbf{COMP532 - Machine Learning and Bio-inspired Algorithms: Assignment I \\ Bandit Arm Problem}}}
%\author{Zhiheng Wang - 201865071 \AND Laura Sierra Pena - 201936067 \AND Ainur Smailova - 201874285 \AND Yan-Yu Lin - 201948160 \AND Saif Ur Rehman \AND Joe Davies - 201950629 \AND Pablo Ramos - 201885602}

\setlength\parindent{10mm}

%%%%%%%%%%%%
%%% BODY %%%
%%%%%%%%%%%%

\begin{document}

%%%%%%%%%%%%%%%%%%%%
% Useful templates %
%%%%%%%%%%%%%%%%%%%%

% ~~~~ Toggle Dark Mode
% Dark Mode - Comment the bottom 2 lines to get a normal white page
%\pagecolor{black}
%\color{white} % Set the default colour to white
% ~~~~ Toggle page numbering
%\pagenumbering{gobble} % Turns off page numbering
% ~~~~ Figure
%\begin{figure}
%  \includegraphics[width=\linewidth]{correlation_matrix.png}
%  \caption{\small Write_your_caption_here}
%  \label{name_your_figure_for_ref}
%\end{figure}
% ~~~~ Equation
%\begin{center}
%  $z = \frac{x-\mu}{\sigma}$ \\ \smallbreak \small \textit{Standard_normalization_removes_the_mean_and_scales_to_unit_variance.}
%\end{center}
%~~~~ Table (It's easier to use a web tool)
%\begin{table}[h]
%\centering
%\caption{Scores of the implemented models}
%\label{model_scores_table}
%\begin{tabular}{@{}llll@{}}
%\toprule
%Model               & Balanced Accuracy & Regular Accuracy & Cross Validation Score \\ \midrule
%Random Forest       & 0.950231          & 0.947761         & 0.955048               \\
%Logistic Regression & 0.783102          & 0.798507         & 0.762543               \\
%SVM                 & 0.743056          & 0.768657         & 0.745699               \\ \bottomrule
%\end{tabular}
%\end{table}
% ~~~~ Display blocky quotes
%\begin{displayquote}
%    This is some text from a book.
%\end{displayquote}

\maketitle

\textit{University of Liverpool} \smallskip \\
 Zhiheng Wang - 201865071 \\ 
 Laura Sierra Pena - l.v.sierra-pena@liverpool.ac.uk - 201936067 \\ 
 Ainur Smailova - 201874285 \\ 
 Yan-Yu Lin - 201948160 \\ 
 Joe Davies - 201950629 \\ 
 Pablo Ramos - sgpramos@liverpool.ac.uk- 201885602 \\

{\setlength\parindent{0mm} \color{ForestGreen} \rule{\linewidth}{0.5mm}} % Lines with colour options

\section*{\LARGE Introduction}
Reinforcement learning is an artificial intelligence approach that uses agents set to take actions in complex environments and learn from their interactions. The objective is to maximise the expected cumulative reward.

As defined by Sutton \& Barto, 2018, Ch.2, Sec 2.1, Multi-armed bandits (MABs) are a simplified reinforcement learning setting where an agent repeatedly chooses among several bandits to maximise reward under uncertainty. 

A multi-armed bandit problems can be interpreted as a casino parlor: multiple slot machines, which are referred to as bandits, are set to give random rewards. The value of these rewards and their likelihood can be hardcoded to behave like a probability density function. I.E. the slot machines, when pulled, may give positive or negative rewards that converge around a mean. That said, many kinds of statistical distributions may be implemented, such as uniform distributions with upper and lower bounds, triangular distributions with a mode, and so on.

Using reinforcement learning, agents must be set with some kind of policy that enables taking the optimal choice among the multiple slot machines, I.E. the bandits, available. Sequentially pulling on different bandits, will influence the agents' decisions by providing evaluative feedback.

A central difficulty is the exploration exploitation trade-off. When action value estimates are maintained, the arm with the highest current estimate is the greedy choice. Selecting it is exploitation because it uses the best option according to current knowledge, while selecting a non-greedy arm is exploration because it can reduce uncertainty and improve future estimates. Exploitation tends to maximise expected reward in the next step, but exploration can lead to higher total reward over a longer horizon by helping the agent discover arms that are actually better. Since a single action choice cannot do both at once, balancing exploration and exploitation is intrinsical in bandit problems.

A common way to implement action value methods is to estimate each arm's value from observed rewards. One simple estimator is the sample average, computed as the total reward collected from an arm divided by the number of times it has been selected. (Antonacopoulos et al., 2025)

To compare bandit methods, performance is often visualised using reward based curves such as cumulative or average reward over time, together with the frequency of selecting the optimal arm. (Antonacopoulos et al., 2025)

In many real applications, reward distributions are non-stationary and can change over time. This motivates methods that adapt to recent observations rather than relying on full historical averages. Recent work has also explored explicit uncertainty measures, for example using the fractal dimension of reward history and integrating it into standard bandit algorithms, with reported improvements in cumulative reward in non-stationary settings. (Nandhini \& Pandian, 2025)

\section*{\LARGE Methodology}


\begin{table}[h]
\centering
\caption{Agents and Environments}
\label{table_1}
\begin{tabular}{lll}
\toprule
Estimator                                  & Strategy      & Environment                      \\
Total Average                              & epsilon = 0.1 & Static (Stationary Bandits)      \\
Total Average                              & epsilon = 0.3 & Static (Stationary Bandits)      \\
Total Average                              & epsilon = 0.5 & Static (Stationary Bandits)      \\
Total Average                              & epsilon = 0.7 & Static (Stationary Bandits)      \\
Total Average                              & epsilon = 0.9 & Static (Stationary Bandits)      \\
Total Average                              & softmax       & Static (Stationary Bandits)      \\
Total Average                              & softmax       & Dynamic (Non-stationary Bandits) \\
Moving Average (window=100)                & softmax       & Dynamic (Non-stationary Bandits) \\
Moving Average (window=20)                 & softmax       & Dynamic (Non-stationary Bandits) \\
Inc Update (step = 0.1)                    & softmax       & Dynamic (Non-stationary Bandits) \\
Epsilon-Greedy (epsilon=0.1, step=0.1) & softmax       & Dynamic (Non-stationary Bandits) \\
Inc Update (step = 0.3)                    & softmax       & Dynamic (Non-stationary Bandits) \\
\bottomrule
\end{tabular}
\end{table}

To approach the problem, we first compared types of agents with different strategies and update rules in different environments, specifically one static environment and one non-static (dynamic) environment, as seen in Table \ref{table_1}.

After analysing the different strategies, we decided object-oriented programming presented an efficient and effective way to implement the multi-armed bandit algorithm. We started by creating the base abstract arm class, with multiple subclasses, each with different distributions, secondly, a base abstract Estimator class which provides estimation of different actions and ways to update that estimation. Thirdly, a base abstract Strategy class, which selects which action to perform based on the estimations, and finally an Agent class, which is the combination of an estimator and a strategy, as well as recording the cumulative reward over time.

In our environment, we have a list of five arms, and ten agents. In each step, each agent selects an arm using its specific strategy. The agent receives a reward from the selected arm, and the agent updates its estimator with that reward. After that, the results are then plotted with cumulative reward in the y-axis and timesteps in the x-axis. The results show a range of cumulative rewards over timesteps, where a lower epsilon value corresponds to a higher cumulative reward.

To ensure reproducibility in between runs, we used NumPy's random seed module and selected different values for the seeds.

\section*{\LARGE Results}
\subsection{Static Environment}

\begin{figure}
  \includegraphics[width=\linewidth]{static_cumulative_reward.jpeg}
  \caption{\small Cumulative reward over time with standard deviation bounds for static environment}
  \label{cumulative_static}
\end{figure}

In Figure \ref{cumulative_static} it can be observed that over many steps, small values for $\varepsilon $ and softmax strategies accumulate reward much faster, leading to clearly higher curves. Strategies with large $\varepsilon $ show significantly lower cumulative reward, which corresponds to higher cumulative regret.

\begin{figure}
  \includegraphics[width=\linewidth]{static_average.jpeg}
  \caption{\small Average reward over time with standard deviation bounds for static environment.}
  \label{average_static}
\end{figure}

The patterns match the best-arm plot: small $\varepsilon$  (0.1, 0.3) and softmax achieve higher steady-state average reward. Large $\varepsilon$ values suffer from excessive exploration, so their average reward stabilizes at a much lower level.
This illustrates that, in a static bandit, too much exploration directly hurts long-run reward.

\begin{figure}
  \includegraphics[width=\linewidth]{static_percentage.jpeg}
  \caption{\small Percentage of best arm selection over time for static environment.}
  \label{percentage_static}
\end{figure}

In Figure \ref{percentage_static} it is shown that in a stationary environment, lower $\varepsilon $ (0.1) quickly converges to selecting the best arm most of the time, giving high optimal-arm percentages. Higher $\varepsilon $ values (0.7, 0.9) keep exploring too often, so their optimal-arm selection rate remains low even after many steps.
The Softmax policy rapidly concentrates most probability on the best arm while still doing some graded exploration, so it ends up with the highest optimal-arm selection.

\subsection{Dynamic Environment}
\begin{figure}
  \includegraphics[width=\linewidth]{dynamic_cumulative.jpeg}
  \caption{\small Cumulative reward over time with standard deviation bounds for dynamic environment}
  \label{cumulative_dynamic}
\end{figure}

All methods increase cumulative reward, but the slopes separate as the environment changes. The average estimator (green) initially performs well but its curve flattens compared to the others, reflecting slower adaptation and more pulls of suboptimal arms. Moving average (yellow) does better, and the incremental update (orange) achieves the steepest curve, the highest long-term cumulative reward, which means lower regret in a non-stationary bandit.

\begin{figure}
  \includegraphics[width=\linewidth]{dynamic_noisy_cumulative.jpeg}
  \caption{\small Cumulative reward over time with standard deviation bounds for dynamic environment}
  \label{cumulative_noisy_dynamic}
\end{figure}

Figure \ref{cumulative_noisy_dynamic} adds more estimators (different windows, step sizes, $\varepsilon $-greedy) and shows them under higher noise. Despite the noise, methods with larger constant steps and exploration ($\varepsilon $-greedy) tend to track changes better and obtain higher cumulative reward, while methods that average over too much history lag behind because old data dominates their estimates.

\begin{figure}
  \includegraphics[width=\linewidth]{dynamic_percentage.jpeg}
  \caption{\small Percentage of Best Arm Selection over time in a Dynamic Environmentt}
  \label{percentage_dynamic}
\end{figure}

At the start, all methods select the best arm rarely, because their value estimates are still very uncertain. The average estimator (green) learns reasonably fast but then degrades as the environment changes, since it keeps all past rewards and therefore reacts slowly to non-stationarity.
The moving average (yellow) reacts faster to changes than the plain average, so the percentage of best-arm selections improves more steadily. The incremental update with fixed step size (orange) adapts the quickest and maintains the highest fraction of optimal-arm selections over time, showing that constant-step updates are more appropriate in a dynamic environment.
Overall, these graphs support the conclusion that in static environments we want low exploration (or Softmax), while in dynamic environments we need estimators and step sizes that adapt quickly to changes.

\begin{itemize}
  \item Experimental results show the average estimator decays over time because it calculates the value with historical memory
  \item Moving average shows lower efficiency at the start because it has lower information/data to work with, but over time it improves significantly due to the dynamic nature of the estimator 
  \item For the incremental estimator, not only did it start off with relatively high optimal selection, but also it performed the most optimally over time. This is because it accumulates data results, and it has more accumulative data to work with. 
  \item These results indicate that dynamic estimators are superior to the average estimator, and the incremental estimator is the most optimal (within our timespan). Given more time, there is a potential for the moving average to overtake the incremental estimator, but this cannot be accurately shown from the data.
\end{itemize}
\section*{\LARGE Discussion}
\subsection*{Problem 2}

The trade-off between exploration and exploitation is a central challenge in reinforcement learning. To obtain high reward, an agent should prefer actions that have produced good results in the past (exploitation), but to discover such actions it must also try actions that have not been tried often or at all (exploration). (Berger-Tal, Nathan, Meron, \& Saltz, 2014). In other words, the agent must exploit what it already knows to gain immediate reward, while also exploring to improve its knowledge and make better action selections in the future. (Sutton \& Barto, 2014)
In multi-armed bandits, the exploration-exploitation dilemma arises because, at each step, the agent must decide whether to play the arm that has produced the highest average reward so far (exploitation) or to try a different arm to improve its estimate of that arm's true value (exploration). Formally, each arm has an unknown reward distribution, and the agent only learns about these rewards through repeated interaction, which makes this trade-off unavoidable.
The goal of a multi-armed bandit agent is to find an appropriate balance between exploration and exploitation that minimises the regret over a given time horizon. Regret is defined as the difference between the cumulative reward that would have been obtained by always selecting the optimal arm and the cumulative reward obtained by the agent's strategy. A good bandit algorithm aims for regret that grows sublinearly with time, so that the average regret per step converges to zero. (Bubeck \& Cesa-Bianchi, 2012) 
One simple strategy to address this trade-off is the epsilon-greedy method. In epsilon-greedy, at each step the agent chooses a random arm with probability $\varepsilon $ (exploration) and chooses the arm with the highest current estimated value with probability  $1-\varepsilon $ (exploitation). The parameter $\varepsilon $ therefore controls the balance between exploration and exploitation: larger values of $\varepsilon $ encourage more exploration, while smaller values favour exploiting the best-known arm. (Sutton \& Barto, 2014)

\subsection*{Problem 3}
The true value of an action is the average reward that was received after that action was taken. The difference between methods lies in how they use that uncertainty to balance exploration and exploitation. In our project we looked at some basic ways to estimate that value and used it to decide what to do next. In terms of static and dynamic environments, we mean the stationary and non-stationary environments.

Key Action-Value methods used:

\subsubsection*{1. Sample-Average Estimator}

This method estimates each arm's value by calculating the mean of all rewards received from that arm. It was tested with epsilon values of 0.2, 0.5, and 0.7....
In static environments it works well, especially when consistent with small epsilon valued. The agent does not need much exploration once it learns the arm values. However, in dynamic environments this estimator becomes a problem because it gives equal weight to old and new rewards, so it cannot adapt quickly with the environment changes.

\subsubsection*{2. Moving Average Estimator}

Instead of using all history, this method only considers the last N rewards (windows of 100 and 20 were used).
At the beginning it performs worse than sample-average because it has less data. But in dynamic environments it improves over time since it forgets old rewards and reacts better to changes. A window of 20 showed slightly better results than a window of 100 in most cases.

\subsubsection*{3. Incremental Update Rule}

Uses the formula $Q_{n+1} = Q_n + \alpha [R_n - Q_n]$ with a fixed step size $\alpha $, which makes recent rewards more important than old ones.
This was the best estimator for dynamic environments. It adapted the fastest to reward distribution changes and kept the highest percentage of optimal arm selections over time. It also started well from the beginning, not only at the end. The team notes that moving average could eventually overcome it given more time, but this was not confirmed by the data.

\subsubsection*{4. $\varepsilon $-Greedy Strategy}

The agent exploits the best known arm with probability $1-\varepsilon $ and explores randomly with probability $\varepsilon $. Values of 0.2, 0.5 and 0.7 were compared.
In static environments, low epsilon (0.1-0.3) was clearly better because excessive exploration hurts performance when arm values do not change. In dynamic environments, moderate exploration was more useful because the agent needs to detect when the best arm changes. Results varied between seeds, which shows the stochastic nature of the problem.

\subsubsection*{5. Softmax Strategy}

Instead of a binary explore/exploit decision, Softmax assigns probabilities to each arm based on learned preferences, updated by gradient ascent.

\subsubsection*{Subtitle: Considerations on Action Value Methods}

Softmax was the best strategy overall for static environments. It concentrated probability on the best arm faster than epsilon-greedy while still doing some exploration. It achieved the highest optimal-arm selection rate among all strategies tested.
In general, static environments need low exploration and stable estimators, while dynamic environments need methods that can forget old data and keep exploring. The incremental update is the most suitable estimator for non-stationary settings.

\section*{\LARGE Conclusions}
We found that the SoftMax strategy was the best performing one on our stationary setup. We found that, when faced with responsive bandits, the best estimator was incremental update, followed by moving average with a window of 20. Total average estimators are very unreliable when dealing with non-static bandits (what we defined as a dynamic environment).

% Implicit citations
\nocite{antonacopoulos_pattern_2025}
\nocite{nandhini_fractal_2025}
\nocite{berger-tal_exploration-exploitation_2014}
\nocite{bubeck_regret_2012}
\nocite{sutton_reinforcement_nodate}
\nocite{wang_exploration_2019}
\printbibliography

\end{document}